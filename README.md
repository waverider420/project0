# project0 / taro

A small, lightweight command-line file archiver that doesn't even compress (for now). Minimal dependencies (this uses my fully custom I/O).

To build, just cd into the repo and run make.
